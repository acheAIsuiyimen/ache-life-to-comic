# 参与项目

谢谢你愿意帮这本书长得更好。

## 先从 Issue 开始

适合提交的内容：

- 可以复现的排版、分页、缺图或跨平台问题；
- 某一种输入没有被正确路由；
- 原图、原文、会议字段或事实没有被完整保留；
- 桌面、手机或打印视图出现视觉错误；
- 不涉及私有素材的改进建议。

请附上最小复现输入、运行环境、期望结果和实际结果。涉及个人照片、会议或账号信息时，先脱敏。

## Pull Request

1. 从 `main` 创建分支。
2. 不改写已验收的设计基线；确需变化时，说明版本号和原因。
3. 运行 `npm test`。
4. 涉及 HTML/CSS 时，同时运行视觉 fixture 和桌面、手机检查。
5. 不提交 `.env`、凭证、绝对本地路径、用户数据或未获授权的素材。
6. 不把 66 大王或画风样张重新授权给第三方。

提交 PR 代表你有权贡献这些内容，并同意项目按仓库现行许可分发它们。是否合并由维护者决定。
